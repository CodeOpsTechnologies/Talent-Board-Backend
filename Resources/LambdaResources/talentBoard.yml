handler: TalentBoard/index.handler
package:
  individually: true
  artifact: "./dist/TalentBoard.zip"
events:
  - http:
      path: /talent
      method: post
      cors: true
  - http:
      path: /talent/profiles
      method: post
      cors: true
  - http:
      path: /talent/filters
      method: get
      cors: true